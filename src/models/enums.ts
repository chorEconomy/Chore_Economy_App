export enum EGender {
  Male = "male",
  Female = "female",
}

export enum ERole {
  Parent = "parent",
  Kid = "kid",
  Admin = "admin",
}

export enum EStatus {
  Active = "active",
  Inactive = "inactive",
  Disabled = "disabled",
}

export enum EChoreStatus {
  Completed = "completed",
  InProgress = "inprogress",
  Unclaimed = "unclaimed"
}

export enum ExpenseStatus {
  Paid = "paid",
  Unpaid = "unpaid"
}