export enum EGender {
  Male = "male",
  Female = "female",
}

export enum ERole {
  Parent = "parent",
  Child = "child",
  Admin = "admin",
}

export enum EStatus {
  Active = "active",
  Inactive = "inactive",
  Disabled = "disabled",
}

export enum EChoreStatus {
  Completed = "completed",
  InProgress = "inprogress",
  Unclaimed = "unclaimed"
}